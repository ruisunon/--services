server:
  port: ${PORT}

spring:
  cloud:
    config:
      server:
        native:
          searchLocations: file://${GIT_REPO} # Use the File System Backend to avoid git pulling. Enable "native" profile in the Config Server.
